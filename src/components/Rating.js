/*import React from "react";

export default function Rating({ data }) {
  return ({
    Array(data)
      .fill()
      .map((_, i) => (
        <p key={i}>
          <span role="img" aria-label="star">
            ⭐
          </span>
        </p>
      ))
  })
}*/
